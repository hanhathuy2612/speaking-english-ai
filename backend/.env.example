# Copy to .env. Loads .env, then .env.$APP_ENV (default development), then .env.local — later overrides. Restart after changes.

LMSTUDIO_BASE_URL=http://localhost:1234
LMSTUDIO_API_KEY=
LMSTUDIO_MODEL=llama-3.2-3b-instruct@q4_k_m

JWT_SECRET_KEY=super-secret-change-me

DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/speaking_english

CORS_ALLOWED_ORIGINS=["http://localhost:4200", "http://192.168.1.44:4200"]

MAX_AUDIO_UPLOAD_MB=10
REQUEST_TIMEOUT_SECONDS=60

# Optional (defaults in app/core/config.py): LM_CONVERSATION_MAX_TOKENS, LM_SYSTEM_PROMPT_EXTRA, TTS_RATE, TTS_VOICE, STT_MODEL_SIZE, STT_BEAM_SIZE, STT_DEVICE, STT_COMPUTE_TYPE, FFMPEG_PATH

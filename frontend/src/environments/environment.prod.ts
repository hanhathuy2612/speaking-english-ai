export const environment = {
  production: true,
  apiBaseUrl: '/api/v1',
  /** Empty = use current host (e.g. wss://same-origin/api/v1). Set to full base URL if API/WS on another host. */
  wsBaseUrl: '',
};
